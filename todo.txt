First login:
* crete user configuration directory
* copy the "template" configuration file
* update the local configuration file with correct paths for ssh keys
  and storage dir
* create ssh keys

first page:
* show the list of running clusters
  - button to stop the cluster
  - button to grow the cluster

"start new cluster" page:
* show a list of cluster templates
* allow to select each one of them and show:
  - the cluster configuration
  - an input box for each "group" of nodes, with the current number of
    nodes configured, and allow you to update (without updating the
    cluster) these values
  - when "start" is clicked: a new cluster is started.

"configure" page:
* 4 sections:
  - cluster
  - cloud
  - login
  - setup
